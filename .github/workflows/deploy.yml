name: Deploy
permissions:
  contents: read

on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  deploy-staging:
    if: startsWith(github.ref, 'refs/heads/main')
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v6
      
      - uses: actions/setup-node@v6
        with:
          node-version: 22
        # Remove cache: npm here if using stickydisk for ~/.npm below

      - name: Mount npm cache
        uses: useblacksmith/stickydisk@v1
        with:
          key: ${{ github.repository }}-${{ runner.os }}-node22-npm-${{ hashFiles('**/package-lock.json') }}
          path: ~/.npm
          
      - run: npm ci
      - name: Type check (fail fast)
        run: npm run typecheck:deploy
      
      - run: npm run build --workspace=@vmp/web
        env:
          API_URL: https://vmp-api.tjm.sk
      
      - name: Deploy API (staging)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN_STAGING }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_STAGING }}
        run: npm run deploy --workspace=@vmp/api
      
      - name: Deploy Web (staging)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN_STAGING }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_STAGING }}
          command: pages deploy packages/web/dist --project-name ${{ secrets.CF_PAGES_PROJECT_NAME }} --branch main
  deploy-production:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v6
      
      - uses: actions/setup-node@v6
        with:
          node-version: 22
        # Remove cache: npm here if using stickydisk for ~/.npm below

      - name: Mount npm cache
        uses: useblacksmith/stickydisk@v1
        with:
          key: ${{ github.repository }}-npm-cache
          path: ~/.npm
  
      - name: Mount node_modules
        uses: useblacksmith/stickydisk@v1
        with:
          key: ${{ github.repository }}-node-modules
          path: ./node_modules
          
      - run: npm ci
      - name: Type check (fail fast)
        run: npm run typecheck:deploy
      
      - run: npm run build --workspace=@vmp/web
        env:
          API_URL: https://vmp-prod-api.tjm.sk
      
      - name: Deploy API (production)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN_PROD }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_PROD }}
        run: npm run deploy --workspace=@vmp/api
      
      - name: Deploy Web (production)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN_PROD }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID_PROD }}
          command: pages deploy packages/web/dist --project-name vmp-web --branch main
