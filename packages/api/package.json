{
  "name": "@vmp/api",
  "version": "1.0.0",
  "private": true,
  "description": "VMP API - Cloudflare Workers with D1",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 execute video-subscription-db --file=./migrations/0001_initial.sql",
    "db:seed": "wrangler d1 execute video-subscription-db --file=./migrations/0002_seed.sql"
  },
  "dependencies": {
    "@vmp/shared": "*"
  },
  "devDependencies": {
    "wrangler": "^4.77.0"
  }
}