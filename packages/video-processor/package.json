{
  "name": "@vmp/video-processor",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Pages admin for R2 upload + processing workflow",
  "scripts": {
    "check": "bash -n scripts/video_pipeline_watch.sh",
    "dev": "wrangler pages dev public --compatibility-date=2025-01-01",
    "deploy": "wrangler pages deploy public --project-name vmp-video-processor-admin --branch main"
  }
}