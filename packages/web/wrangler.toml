# DEPRECATED — Cloudflare Pages is no longer used for VMP frontend deploys.
# Canonical frontend: wrangler.workers.toml + npm run deploy (Workers).
# Kept only for reference if you need a one-off Pages build (`npm run build:pages`).

name = "vmp-fe"
pages_build_output_dir = "dist"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
