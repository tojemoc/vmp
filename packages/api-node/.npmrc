# Standalone installs (Deno Deploy, CI) must not hoist to the monorepo root (avoids Nx/Nuxt postinstall).
workspaces=false
# Node 24+ V8 headers require C++20 when better-sqlite3 falls back to node-gyp.
cxxflags=-std=c++20
