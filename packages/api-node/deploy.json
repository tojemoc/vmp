{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "VMP api-node failover — deploy via GitHub Actions (Blacksmith prebuild), not Deno built-in monorepo install.",
  "nodeVersion": "24.11.0",
  "rootDirectory": "packages/api-node",
  "githubActions": {
    "denoDeployOrg": "tjm",
    "org": "tjm",
    "denoDeployApp": "vmp",
    "secret": "DENO_DEPLOY_TOKEN",
    "buildOnRunner": [
      "node scripts/deploy-install.mjs",
      "npm run build",
      "node scripts/deploy-prune-prod.mjs"
    ],
    "upload": "node scripts/deno-deploy-upload.mjs"
  },
  "runtimeEntrypoint": "dist/server.js",
  "notes": "In console.deno.com set the linked repo to GitHub Actions mode (disable automatic install/build). CI builds dist/ + production node_modules on Blacksmith, then deno deploy --allow-node-modules uploads the prebuilt tree."
}
