{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Hints for PaaS builders deploying only the api-node failover (not the full monorepo web stack).",
  "nodeVersion": "24.11.0",
  "rootDirectory": "packages/api-node",
  "installCommand": "npm ci",
  "buildCommand": "npm run build",
  "startCommand": "node dist/server.js",
  "runtimeEntrypoint": "dist/server.js",
  "notes": "Run all commands with working directory packages/api-node. Uses packages/api-node/package-lock.json (no monorepo root install). Build bundles ../api and ../shared sources via esbuild; runtime is dist/server.js only."
}
