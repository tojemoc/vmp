{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "VMP api-node — Deno Deploy via GitHub Actions prebuild; Postgres via DATABASE_URL.",
  "nodeVersion": "24.11.0",
  "rootDirectory": "packages/api-node",
  "githubActions": {
    "denoDeployOrg": "tjm",
    "org": "tjm",
    "denoDeployApp": "vmp",
    "secret": "DENO_DEPLOY_TOKEN",
    "buildOnRunner": [
      "npm run build"
    ],
    "upload": "node scripts/deno-deploy-upload.mjs"
  },
  "runtimeEntrypoint": "dist/server.js",
  "notes": "In console.deno.com set the linked repo to GitHub Actions mode (disable automatic install/build). CI builds a self-contained dist/ bundle on Blacksmith, then deno deploy uploads the prebuilt tree."
}
