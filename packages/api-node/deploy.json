{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Hints for PaaS builders deploying only the api-node failover (not the full monorepo web stack).",
  "nodeVersion": "24.11.0",
  "installCommand": "NX_SKIP_POSTINSTALL=1 npm run install:api-node",
  "buildCommand": "npm run build:api-node",
  "startCommand": "node packages/api-node/dist/server.js",
  "runtimeEntrypoint": "packages/api-node/dist/server.js",
  "denoConfig": "../../deno.json",
  "notes": "Do not use src/server.ts as the runtime entry — sources use Node ESM .js specifiers and are bundled by esbuild. Deno Deploy must run dist/server.js after build; deno.json enables sloppy-imports only for the dependency-cache step."
}
