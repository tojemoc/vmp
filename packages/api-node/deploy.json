{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "VMP api-node — Deno Deploy via linked GitHub repository; Postgres via DATABASE_URL.",
  "nodeVersion": "24.11.0",
  "rootDirectory": "packages/api-node",
  "runtimeEntrypoint": "dist/server.js",
  "notes": "Deno Deploy builds from the linked repo on every push (PR previews and production on main). GitHub Actions does not run deno deploy CLI uploads — those fail consistently while git-triggered builds succeed. PR gate: npm run verify:api-node in ci.yml. Deploy gate: Deno Deploy build status on the PR."
}
