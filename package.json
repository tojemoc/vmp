{
  "name": "vmp",
  "version": "1.0.0",
  "private": true,
  "description": "Video Monetization Platform - HLS video streaming with subscription-based access control",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "deploy:api": "npm run deploy --workspace=@vmp/api",
    "deploy:web": "npm run deploy --workspace=@vmp/web",
    "db:create": "cd packages/api && wrangler d1 execute video-subscription-db --file=./migrations/0001_initial.sql",
    "db:seed": "cd packages/api && wrangler d1 execute video-subscription-db --file=./migrations/0002_seed.sql",
    "deploy:video-processor": "npm run deploy --workspace=@vmp/video-processor"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "overrides": {
    "serialize-javascript": ">=7.0.3",
	"brace-expansion": ">=5.0.5"
  }
}
